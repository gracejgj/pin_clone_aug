<% turbo_frame_tag dom_id(board) do %>
  <br>
  <%= link_to "#{board.name}", board_path(board), 'data-turbo-frame': '_top' %>
<% end %>

<%# partial file - show ever single boards created %>
<%# 'data-turbo-frame': '_top' means jump out from the frame%>
