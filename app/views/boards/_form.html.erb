<%# turbo_frame_tag dom_id(@board) do %>
  <%= form_with(model: board) do |f| %>
    <%# render 'shared/error_message', object: board %>
    <%= f.label :name %>
    <%= f.text_field :name %>
    <%= f.submit 'Save' %>
  <%# end %>
<% end %>
